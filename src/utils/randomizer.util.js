/**
 *
 *  Returns random number between 1 and 351
 *  @returns {Number} - The random number
 */
export function getRandomNumber() {
  return Math.floor(Math.random() * 351) + 1 //only until 3rd gen :)
}
